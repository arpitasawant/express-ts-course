import { userResponses } from "../users/user.responses";

export const AuthResponses = {
    "INVALID_CREDENTAILS":{
        statusCode:400,
        message:"Invalid details"
    },
    "INVALID_ID":{
        statusCode:400,
        message:"invalid id"
    }

}